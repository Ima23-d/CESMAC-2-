#include <stdio.h>
#include <locale.h>
void calcular(int lado){
    printf("O perimetro do quadrado é igual: %d", (lado*2) + (lado*2));
}
int main()
{   setlocale(LC_ALL,"");
    int lado;
    printf("Digite o valor do lado\n ");
    scanf("%d",&lado);
    while (lado <0){
        printf("Digite valores positivos\n");
        printf("Digite o valor do lado\n ");
        scanf("%d",&lado);
    }
    calcular(lado);

    return 0;
}