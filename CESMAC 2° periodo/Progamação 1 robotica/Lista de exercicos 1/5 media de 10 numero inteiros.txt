#include <stdio.h>
#include <locale.h>
int main() {
    setlocale(LC_ALL, "");
    int soma = 0;
    float media;

    for (int i = 0; i <= 10; i++) {
        printf("%d\n", i);
        soma += i;
    }

    media = soma / 10.0;
    printf("Soma = %d\n", soma);
    printf("Média = %.2f\n", media);

    return 0;
}